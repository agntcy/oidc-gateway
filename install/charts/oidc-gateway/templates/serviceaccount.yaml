{{/*
Copyright AGNTCY Contributors (https://github.com/agntcy)
SPDX-License-Identifier: Apache-2.0
*/}}

{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "oidc-gateway.serviceAccountName" . }}
  labels:
    {{- include "oidc-gateway.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
# Separate service account for Envoy (for SPIFFE ID)
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "oidc-gateway.envoyServiceAccountName" . }}
  labels:
    {{- include "oidc-gateway.envoyLabels" . | nindent 4 }}
{{- end }}
